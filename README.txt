morsetalk_gui func is a module which will 
create a graphical user interface for the 
project.now it has basic morsetalk features
like:ENCODE & DECODE .the func uses a dynamic 
result printng so that what the user types in the 
entry fields the function dynamically prints in the 
output in the window.this func is very interactive
and user friendly.the function can be integrated with 
more features of this project.



Author:aribis369
